[package]
name = "apk-analyzer-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for building APK Analyzer plugins"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
