# 2026-07-03 Work Log

## APK Analyzer Project - Initial Build

### Completed
- Installed Rust toolchain (1.96.1) in project-local `.cargo/` and `.rustup/`
- Scaffolded Tauri 2 + React + TypeScript project structure
- Implemented pure Rust APK parsing core:
  - Binary AndroidManifest.xml (AXML) decoder
  - APK Signing Block parser for V2/V3 certificates
  - X.509 certificate parsing via x509-parser crate
  - DEX file parser with ULEB128 string table
  - resources.arsc parser
  - ZIP archive reader
- Implemented 10 analyzers (Overview, Manifest, Permissions, Components, Resources, NativeLibs, DEX, Certificate, Security, AISummary)
- Built permission database with 40+ known Android permissions
- Built tech stack detector (Flutter, React Native, Unity, Kotlin, Compose, etc.)
- Built Tauri command layer with async progress events
- Built React frontend:
  - Home page with drag-and-drop and recent files
  - Main layout with sidebar navigation, toolbar, progress bar
  - 10 analysis pages with data tables, search, expandable rows
  - Global search overlay
  - Export dialog (JSON/MD/HTML/CSV)
  - Dark/light theme support
- Export module (JSON, Markdown, HTML, CSV)
- Verified: `cargo build` succeeds, `tsc --noEmit` clean, `vite build` succeeds
- Generated RGBA PNG icons for Tauri bundling

### Key Challenges Solved
- x509-parser 0.16 API differences (serial access, OID method vs field)
- Tauri proc macro requires RGBA format PNG icons
- AXML binary XML format parsing (string pool, resource map, XML chunks)
- APK Signing Block parsing (V2/V3 certificate extraction)
- Rust borrow checker issues (lifetimes on find_all iterator, moved values)
- Sandbox memory limits (used `cargo build -j 1` and `NODE_OPTIONS=--max-old-space-size=512`)
